<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        /* Existing styles remain unchanged */
        /* ... */
    </style>
    <link rel="stylesheet" href="css/style.css"> <!-- Keeping the original stylesheet -->
</head>
<body>
    <div class="container">
        <h2 style="font-size: 20px; color: green;">Register</h2>
        <% if (typeof errorMessage !== 'undefined') { %>
            <div class="alert alert-danger" role="alert" style="font-size: 20px; color: green;">
                <%= errorMessage %>
            </div>
        <% } %>
        <form action="/register" method="POST">
            <div class="form-group">
                <label for="email" style="font-size: 20px; color: green;">Email:</label>
                <input type="email" id="email" name="email" required class="form-control" style="font-size: 20px; color: green;">
            </div>
            <div class="form-group">
                <label for="username" style="font-size: 20px; color: green;">Username:</label>
                <input type="text" id="username" name="username" required class="form-control" style="font-size: 20px; color: green;">
            </div>
            <div class="form-group">
                <label for="password" style="font-size: 20px; color: green;">Password:</label>
                <input type="password" id="password" name="password" required class="form-control" style="font-size: 20px; color: green;">
            </div>
            <div class="form-group">
                <label for="domainOfInterest" style="font-size: 20px; color: green;">Domain of Interest:</label>
                <select id="domainOfInterest" name="domainOfInterest" required class="form-control" style="font-size: 20px; color: green;">
                    <!-- Options remain unchanged -->
                    <!-- ... -->
                </select>
            </div>
            <div class="form-group">
                <label for="linkedinUrl" style="font-size: 20px; color: green;">LinkedIn URL:</label>
                <input type="url" id="linkedinUrl" name="linkedinUrl" placeholder="https://linkedin.com/in/yourprofile" required class="form-control" style="font-size: 20px; color: green;">
            </div>
            <div class="form-group">
                <label for="currentCompany" style="font-size: 20px; color: green;">Current Company:</label>
                <input type="text" id="currentCompany" name="currentCompany" placeholder="Your Current Company" required class="form-control" style="font-size: 20px; color: green;">
            </div>
            <div class="form-group">
                <label for="currentLevel" style="font-size: 20px; color: green;">Current Level:</label>
                <select id="currentLevel" name="currentLevel" required class="form-control" style="font-size: 20px; color: green;">
                    <!-- Options remain unchanged -->
                    <!-- ... -->
                </select>
            </div>
            <button type="submit" class="btn btn-primary" style="font-size: 20px; color: green; background-color: #007bff;">Register</button>
        </form>
        <p style="font-size: 20px; color: green;">Already have an account? <a href="/login" style="color: green;">Login here</a>.</p>
    </div>
    <script src="js/interactivity.js"></script> 
</body>