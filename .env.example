# .env.example

# MongoDB connection string, replace <username>, <password>, and <dbname> with your actual MongoDB credentials
DB_CONNECTION_STRING=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<dbname>

# Session secret, replace 'your_secret' with a strong secret string
SESSION_SECRET=your_secret

# Add other API keys or sensitive information below with placeholders
# Example API_KEY=your_api_key_here